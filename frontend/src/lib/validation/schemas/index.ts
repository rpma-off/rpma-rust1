/**
 * Zod schemas for frontend types
 *
 * These schemas mirror the TypeScript types in `@/types/` and are used to
 * validate IPC responses, form inputs, and backend response payloads at runtime.
 */

export * from './calendar';
