/**
 * @deprecated Use `@/domains/auth` instead.
 */

export { AuthProvider, AuthContext, useAuth } from '@/domains/auth';
export type { AuthContextType, AuthState, AuthResponse, UserSession, UserAccount } from '@/domains/auth';
